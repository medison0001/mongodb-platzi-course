use("platzi_store")

db.products.updateOne(
    // query
    { _id: 2 },
    // change => operators
    {
        $set: {
            name: 'Change name',
            price: 400,
            tags: ['a', 'b', 'c']
        }
    }
);